# Constant provenance -- if a value changes in code, re-render the GIF that quotes it.
#
# Clip order matches README order AND the rail order in style.py:
#   g00 -> g01 -> g02 -> g03 -> g04 -> g05 -> g06 -> g07 -> g08 -> g09 -> g10 -> g11 -> g12

| GIF    | Content                    | Constant                     | Value                     | Source |
|--------|------------------------------|------------------------------|---------------------------|--------|
| g00    | climate-fields preamble    | display-only: full-octave fields, 2-98 percentile stretch; EXT 4,800 blocks, PITCH 48 | | make_g00_preamble.py |
| g01    | octave ladder              | octave ladder nmax = 1 / 2 / 4 / -1; blob boundary at erosion 0B <= -0.35. NOTE: the CPU blob gate uses the SAME 0B truncation as the GPU (double precision, +-48,000 reach) -- full octaves are what Minecraft/CV use, not a separate "CPU ground truth" | | common.h, probe.cpp blob_fill, fields.py |
| g02    | anchor lattice             | hex lattice 399 anchors (21 rows x 19 cols), spacing 1,920 blocks, covering radius 1,920/sqrt(3) = 1,109 blocks; compared against a 21x21 / 1,600-step square grid (441 anchors, covering radius 800*sqrt(2) = 1,131 blocks) | | gpu.cu KernelCoverage |
| g03    | probe vote                 | PROBE_GATE -0.30 / PROBE_NEED 6 of 7; field = 0B erosion; 7 points = 1 center + 6 edge midpoints 960 blocks out; each midpoint shared by exactly 2 anchors -> 4*399 = 1,596 shared samples per seed | | gpu.cu KernelCoverage |
| g04    | disc scan                  | disc r = 1,600 blocks; 96 samples = 3 phyllotaxis covers x 32 (fail-fast between covers); need 77 (80%); 0B erosion <= -0.40; emitted anchor = centroid of hit samples | | noise_common.h, gpu.cu KernelCoverage |
| g05    | window checks              | (6) temp 121 pts (11x11 @ 100-block pitch, 1,000-block window), need 79 in [-0.40, 0.20], FULL octaves; (4) cont disc 64/96 >= 0, 1B truncation; (w) ridge band \|w\| in [0.55, 0.82] >= 1.2% on (2,2) weirdness @ 100-block pitch (valley term disabled); (3) ero min <= -1.15 / (5) cont max >= 0.70, FULL octaves on 51x51 @ 64-block pitch (3,200-block window), early-exit at first hit; (h) max approx height >= 215 on 25x25 @ 128-block pitch via the depth spline (shift noise skipped) | | gpu.cu KernelExtrema |
| g06    | flood fill                 | BLOB_* field: ero0B <= -0.35 AND cont1B >= -0.12 AND temp0B in [-0.45, 0.20]; 64-block lattice, GPU reach +-8,000 blocks; wavefront BFS; 1-cell closing (dilate-any, erode-all); chamfer-DT core radius; PER_SEED_CAP 16; edge-clipped fills pass at half target; tier-2 height/dh stats are measure-only (BLOB2 gates off) | | common.h, gpu.cu KernelBlob |
| g06    | flood fill gates           | GPU early-exit target = floor(0.88 * min_blob_area / 4096) cells (= 3,867 cells at the 18M default) / CPU gates MIN_BLOB_AREA 18M blocks^2 (default, --min-blob-area) and MIN_CORE 20 cells | | main.cpp, common.h |
| g07    | dark-forest veto           | r = 1,365 blocks, 64-block pitch, reject at >= 7% dark forest | | cpu.cpp verify_candidate |
| g08    | CPU blob + height gates    | CPU blob re-measure: same BLOB_* field, double precision, reach +-48,000 blocks; height window y in [230, 320], square spiral r = 1,600, step 64, radial clip; pass = any sample in window OR observed [min, max] straddles the window | | probe.cpp BLOB_MAX_R, cpu.cpp height_scan |
| g09    | pattern windows            | window_pattern_score = 1.00*z(dh;36,6) + 0.70*z(hstd;40,12) + 0.40*z(hi;0.10,0.10 clamped [-1.5,2]) + 0.35*z(lo;0.02,0.03 clamped [-1.5,2]) + 0.35*z(-ero;0.90,0.15) + 0.30*z(min(mtn,0.62);0.35,0.15) - 3.00*ocn - edge penalty if cover < 0.85; windows skipped when blob cover < 0.5 or ocn > 0.15 (OCN_SKIP); tiers 896/1664/3,200 blocks (R = 3/6/12 sublattice cells @ 128-block pitch) | | probe.cpp window_pattern_score |
| g10    | composite score            | score = 2.00*min(amp, tex, frag, depth, dark) + 0.30*(sum of the five) + 1.10*extent + 1.25*legacyMean - 40*max(0, bw1664Dark - 0.055) - 3*max(0, highLargest - 0.55) + setting (cap 0.80, core-gated: 0 below key 0.5, full above 1.0) | | probe.cpp compute_score, seedlab.py mirror |
| g11    | funnel                     | all stage counts from one real 5 s --debug window of: ./mountain_rangefinder --threads 32 --debug --min-blob-area 20000000 (so the blobfill early-exit is 4,296 cells ~= 17.6M blocks^2 and the CPU area gate is 20M, NOT the 18M defaults; re-measure per release) |
